You are resuming work on SY-Hub (`Rhizzo-ai/SY-Hub`), a single-tenant-live FastAPI (SQLAlchemy 2.x + Alembic) / PostgreSQL / React company-OS for a UK property developer. You are executing **B88 — Commercial Spine — Pack 3.5**: a three-value package-kind vocabulary, a CIS-aware award money-routing change, a bidirectional package↔order link, and a cost-code-grouped package-lines view. The design is LOCKED; do not re-litigate it. Build to a bulletproof, state-of-the-art standard. Money traceability is sacred: every awarded pound must remain resolvable estimate → package line → award line → downstream order line, with zero regression to existing Decimal(14,2) `_q2` award maths.

Repo ground truth at start (verify before touching anything; raw.githubusercontent has been observed STALE — cross-check via the codeload tarball `https://codeload.github.com/Rhizzo-ai/SY-Hub/tar.gz/refs/heads/main`): alembic head `0047_packages` (no `0048` yet), `PACKAGE_KINDS` in `backend/app/models/packages.py:30` still `("labour","materials")`, permissions **142**, roles **10**, cost codes **133**. Routers carry no internal prefix; they mount on `v1_router` (`/v1`) → `api_router` (`/api`), so all commercial surfaces are under `/api/v1` — EXCEPT the cost-codes router, mounted directly under `/api`. Treat the live OpenAPI as authoritative for every path. Brand teal `#0F6A7A`, orange `#FC7827`.

Before writing a single line, re-read these at HEAD line-by-line — never from assumption: `backend/app/models/packages.py` (PACKAGE_KINDS, the `_package_kind_enum` PGEnum, the `package_lines.cost_code` String(20) column); `backend/app/services/packages.py` (`create_package` validation ~327; `_supplier_kind_guard` ~731 and its two callers `invite_bidder` ~763 and `enter_bid` ~990; the award routing branch ~1174/1206/1228; `_ser_package_line` ~1384; `serialise_package` ~1495); `backend/app/services/purchase_orders.py::create_po` (~200, the `PurchaseOrder(...)` constructor ~310, the `serialise` dict); `backend/app/services/subcontracts.py::create_subcontract` (~252, `cis_applies: bool = True` default ~264, the LD2 `supplier_type != "Contractor"` hard-reject ~289, the `Subcontract(...)` constructor ~344, `serialise` ~164); `backend/app/models/purchase_orders.py` and `backend/app/models/subcontracts.py` (confirm neither carries `package_id` yet); `backend/app/routers/purchase_orders.py::POCreate` (~62, note `model_config = ConfigDict(extra="forbid")`) and `backend/app/routers/subcontracts.py::SubcontractCreateBody` (~46); `backend/alembic/versions/0047_packages.py` (the `ALTER TYPE ... ADD VALUE IF NOT EXISTS` autocommit_block pattern ~50-57 and the named CHECK `ck_packages_kind_values` ~157); `frontend/src/lib/budgetCategoryGroup.js` (keyed by `cost_code_id` via a Map — you must NOT reuse this for package lines); `frontend/src/hooks/costCodes.js`; `backend/app/models/cost_codes.py` (`CostCode.name` ~70 is the title); and the package frontend at `frontend/src/components/packages/NewPackageDialog.jsx`, `frontend/src/pages/admin/PackagesList.jsx`, `frontend/src/pages/admin/PackageDetail.jsx`, plus `frontend/src/pages/projects/PurchaseOrderForm.jsx` / `PurchaseOrderList.jsx`.

The full numbered specification with exact instructions, code snippets, constraint/index names, and the per-gate STOP criteria is in the Build Pack document `B88_Pack3.5_Build_Pack.md` — follow it precisely, gate by gate, in order. Do not auto-advance past a STOP. The 2nd-run WARM-DB suite count is authoritative (first run on a fresh pod throws ~IntegrityErrors that clear on the second run). Name every test file and function EXACTLY as the Build Pack specifies; no single-file consolidation.

Critical execution invariants for this pack: (1) The `ALTER TYPE package_kind ADD VALUE IF NOT EXISTS 'subcontract'/'consultant'` statements go in `op.get_context().autocommit_block()`; the `UPDATE packages SET kind='subcontract' WHERE kind='labour'` runs in the migration's normal transaction AFTER that block closes — the new enum value is not usable in the same transaction that adds it. `labour` stays as an orphaned enum member (precedent 0020/0047); the live allowed set is enforced by dropping and recreating the named CHECK `ck_packages_kind_values` to `kind IN ('materials','subcontract','consultant')`. (2) Add NO new permission — the count must remain exactly 142; `test_packages_service.py`'s count assertion must pass untouched. (3) `materials → PO` routing is unchanged; the existing `labour` branch is RENAMED to `subcontract` (same `create_subcontract` call, `cis_applies` default True); a NEW `consultant` branch routes to PO (Consultants cannot be subcontract counterparties — LD2 rejects them — and professional fees are CIS-clean because the PO path applies no CIS). (4) Add a nullable `package_id` UUID column with `ondelete="SET NULL"` FK and an index to BOTH `purchase_orders` and `subcontracts` (named `fk_/ix_<table>_package_id`); deleting a package must null the link, never cascade-destroy a financial order. Thread `package_id` into both downstream creates on the award path, and expose it (plus `package_reference`) on both read serialisers. (5) For the package-lines cost-code grouping, build a NEW `frontend/src/lib/packageLineGroup.js` keyed on the `cost_code` STRING (package lines have no `cost_code_id`), with display-only per-cost-code net subtotals and a numeric-aware dotted-code sort ("4.02" before "4.10"); surface the cost-code title via a new always-present `cost_code_name` field on the serialised line, built from a single `SELECT code, name WHERE code = ANY(:codes)` in `serialise_package` — no per-line query, no frontend string-keyed fetch.

Gate verification is LIVE-API, not suite counts alone. At the money gate (Gate 3) you must, against the running pod with authenticated requests on live OpenAPI paths, create and award a package of each of the three kinds end-to-end and paste the actual JSON: a `consultant` package awarded to a Consultant supplier produces a PO with `package_id` populated and the award's `created_subcontract_id` NULL; a `subcontract` package awarded to a Contractor produces a subcontract with `package_id` populated and `cis_applies` true; a `materials` award produces a PO with `package_id` populated. At Gates 4–7, demonstrate the live behaviour (standalone PO with and without `package_id`; the grouped/subtotalled/titled package-lines view rendering on the dev server; the create dialog offering all three kinds; the invite-bidder picker filtering to the correct supplier type per kind with a server-side rejection visibly surfaced on a wrong type; the "New order" front-door chooser routing to the unchanged Simple-order and Package flows; and the bidirectional "from package X" / created-order links). Mutation handlers must surface errors visibly.

On completion: write the CHANGELOG entry (Pack 3.5 — enum + data migration, `_supplier_kind_guard` flip, consultant→PO routing, `package_id` links, grouping/titles UI, one-front-door shim, with every deviation listed) and `docs/chat-summaries/chat-55-closing.md`. Do NOT write to `docs/SY_Hub_Phase2_Backlog.md` — it is operator-hand-edited only. Two commits (code+tests, then docs); both pushed via Save-to-GitHub; on conflict use Branch & Push → PR → merge, never Force Push; then wait a full 60s and verify every changed file landed on `main` against the codeload tarball (not raw). Final state: alembic head `0048_package_kind_3value_and_links`, permissions 142, roles 10, cost codes 133, `SELECT DISTINCT kind FROM packages` shows only `materials`/`subcontract`/`consultant`, `/api/health` 200, full suite green on 2nd-run WARM-DB with all added tests accounted for against the stated baseline.

If the pod has recycled, recover first: `bash /app/scripts/provision_postgres.sh` then `/root/.venv/bin/python -m app.bootstrap`, reseed, and confirm health (133 cost_codes, 142 permissions, alembic head 0047) before starting. Begin at Gate 1 and report at each STOP.
